//let cpuTeam = require('fight');  // imports?
//let userTeam = r....

// Get User name and display
username = prompt("Winner! What is your name?");


// Grab health and display/store
let yourHealth = user.health;
alert(`Health remaining after battle ${yourHealth}`);


//display defeated team
alert(`You defeated ${cpu.team}`);

module.exports = {
    username,
}